# Current Feature: i18n Support (EN / FR / PL)

## Status

Implemented — pending review / commit. `npm run build`, `npm run lint` (src clean; 2
pre-existing errors in `prototypes/redesign/support.js` only), and `npm run test`
(273 pass, incl. 26 new) all green.

### What shipped

- `next-intl@4` wired cookie-only (no URL locale): `next.config.ts` plugin,
  `src/i18n/request.ts` (`getRequestConfig` — cookie → `Accept-Language` →
  `en`), `src/lib/i18n.ts` (`resolveLocale`/`matchAcceptLanguage`/`isLocale`,
  `SUPPORTED_LOCALES`, `LOCALE_COOKIE`).
- Root `layout.tsx` → async, `<html lang={locale}>`, `NextIntlClientProvider`,
  localized `generateMetadata`.
- `src/messages/{en,fr,pl}.json` — ~40 namespaces; `en` is source, `fr`/`pl`
  machine-quality drafts. `messages-parity.test.ts` asserts identical key sets.
- Persistence: `User.locale String?` (migration `add_user_locale` on the
  `development` branch), `setLocale` server action (cookie + DB), `auth.ts`
  exposes `session.user.locale`, `LocaleSync` reconciles cookie vs. account
  preference once on `(app)` mount.
- `LanguageSwitcher` (`select` variant in `HomeNav`; `segmented` EN|FR|PL in
  `UserFooter` beside the theme control).
- `src/lib/toast.ts` status words ("Success"/"Error") localized via
  `ToastI18nProvider` (plain module → bridged by a render-phase setter).
- `format-date.ts` gained a `locale` arg (deterministic, UTC-pinned);
  `relative-time.ts` gained `relativeTimeParts()` for i18n callers (old
  `formatRelativeTime` kept for its existing test).
- Every user-facing component/page swept: homepage, all auth, app chrome
  (topbar/sidebar/footer/tabbar/search), dashboard, item/collection/favorites
  lists + cards, item drawer + all forms/editors/upload/AI buttons, settings,
  profile, upgrade, 404.

### Deliberately out of scope this pass (flag if wanted)

- System item-type names (`ItemType.name`: "Snippet"/"Prompt"/…) stay English
  per the `AskUserQuestion` decision — they drive routes/slugs. Appears in the
  sidebar, drawer header, "By Type" breakdown, favorites row badge, etc.
- Server-action / API-route `result.error` strings stay English; only their
  client-side fallbacks (`?? t(...)`) and all happy-path toasts are localized.
- `src/lib/file-constraints.ts` validation messages (lib) — English.
- `src/components/ui/*` internals (`pagination.tsx` sr-only "More pages" /
  "Go to previous page" aria) — off-limits per design-system rules; visible
  "Previous"/"Next" IS localized via the `text` prop.
- Emails (`src/lib/email.ts`) not localized.
- `ChaosToOrder` brand names + its dashboard-mock type labels stay English
  (decorative). PL plural grammar on a few count strings is approximate.

## Goals

- Add internationalization for three locales: English (`en`, default), French (`fr`),
  Polish (`pl`).
- Detect the visitor's preferred language from the browser (`Accept-Language`) on first
  visit; fall back to `en` when nothing matches.
- Persist an explicit choice: a `locale` cookie for everyone, plus a `User.locale` column
  for signed-in users so the choice follows them across devices.
- Translate **all user-facing free-form text** in one pass — marketing homepage, every
  auth page, app chrome (nav / sidebar / topbar / user menu), every screen, all
  dialogs/drawers, and server-action error + toast strings.
- Provide a language switcher in the marketing nav (`HomeNav`) and in the authenticated
  app's `UserFooter` dropdown.

## Notes

### Decisions (via `AskUserQuestion`, 2026-08-27)

1. **Library: `next-intl`, cookie-based, NO locale in the URL.** Locale is stored in a
   cookie (and `User.locale` for authed users), not a path segment. Keeps every existing
   route group, hardcoded `redirect()`, `Link`, and `proxy.ts` matcher untouched — a
   `[locale]` segment would ripple through all of them for little gain on a portfolio app.
   Uses next-intl's official "App Router without i18n routing" mode.
2. **Scope: everything user-facing in one pass** (no phased deferral).
3. **System / seeded labels stay in English.** `ItemType.name` ("Snippet", "Prompt",
   "Command", …) is left as-is — it drives routes/slugs and is canonical English. Only
   free-form UI text is translated. Accepted visible inconsistency: type names render in
   English in all three languages.
4. **Translations: Claude drafts all three catalogs.** `en.json` is the source of truth;
   `fr.json` / `pl.json` are machine-quality drafts for the user to review. A parity test
   guards against missing/extra keys.

### Non-obvious constraints from the codebase

- `src/app/layout.tsx` currently hardcodes `<html lang="en">` and is a sync component —
  it must become `async` to read the active locale.
- `src/lib/format-date.ts` deliberately pins locale + `timeZone: 'UTC'` to dodge
  server/client hydration mismatches. Locale-aware dates must go through a deterministic
  formatter (next-intl's `getFormatter()` / `useFormatter()` is SSR-stable given a fixed
  locale) — do NOT reintroduce bare `toLocaleDateString()`.
- Toasts already funnel through `src/lib/toast.ts` (generic "Success"/"Error" title +
  caller message as description). Callers pass raw strings today; each call site needs a
  `t()` key, and the two title words move into the catalog.
- Server actions run with request context, so `getTranslations()` works there for Zod /
  error messages.
- `next-themes` + `useHasMounted` is the established pattern for a client control that
  must not cause a hydration mismatch — the language switcher follows it.

## Implementation Plan

### 1. Setup

- Branch `feature/i18n`.
- `npm i next-intl`.
- `next.config.ts`: wrap export with `createNextIntlPlugin('./src/i18n/request.ts')`.

### 2. Locale core

- `src/lib/i18n.ts` (in `src/lib/` so vitest picks it up): `SUPPORTED_LOCALES =
  ['en','fr','pl']`, `DEFAULT_LOCALE = 'en'`, `Locale` type, `isLocale()` guard, and a
  pure `resolveLocale({ cookieValue, acceptLanguage })` — cookie wins, else best
  `Accept-Language` match, else default. Fully unit-testable.
- `src/i18n/request.ts`: `getRequestConfig` — read the `locale` cookie via
  `next/headers`; if absent, read `Accept-Language` via `headers()` and run
  `resolveLocale`. Load the matching `src/messages/{locale}.json`.

### 3. Provider + metadata

- `src/app/layout.tsx` → `async`; `const locale = await getLocale()`, set
  `<html lang={locale}>`, wrap children in `NextIntlClientProvider`
  (`messages={await getMessages()}`).
- Localize `generateMetadata` `title` / `description`.

### 4. Message catalogs

- `src/messages/en.json` (source), `fr.json`, `pl.json`.
- Namespaces: `common`, `nav`, `auth`, `dashboard`, `items`, `collections`, `favorites`,
  `settings`, `profile`, `upgrade`, `search`, `drawer`, `toasts`, `errors`, `homepage`.

### 5. String extraction (bulk of the work)

- Sweep ~100 components in `src/components/**` + 16 route files + server-action
  strings in `src/actions/**`.
- Server components / pages / actions: `getTranslations()`.
- Client components: `useTranslations()`.
- `src/lib/toast.ts` call sites: swap literals for `t()` keys; move "Success"/"Error"
  titles into `toasts` namespace.
- Do NOT touch DB-derived content (item titles, collection names) or `ItemType.name`.

### 6. Language switcher

- `src/components/i18n/LanguageSwitcher.tsx` (client): a small `Select` (EN / FR / PL),
  `useHasMounted`-guarded, calls the `setLocale` action then `router.refresh()`.
- Placements: `HomeNav` (beside the theme toggle) and `UserFooter` dropdown (new
  "Language" row under the existing "Appearance" section). Style/placement details
  confirmed via `AskUserQuestion` at implementation time, matching prior nav-control work.

### 7. Persistence

- `prisma/schema.prisma`: add `locale String?` to `User`;
  `prisma migrate dev --name add_user_locale` on the **development** branch.
- `src/actions/i18n.ts`: `setLocale(locale)` — validate against `SUPPORTED_LOCALES`,
  `cookies().set('locale', …)`, and if there's a session user, `prisma.user.update`.
  Returns `ActionResult`.
- `src/auth.ts`: load `user.locale` into the token/session; expose `session.user.locale`.
- Sign-in reconciliation (the fiddly bit): after sign-in the cookie may disagree with
  `User.locale`. Handle with a tiny client effect that calls `setLocale(session.user.locale)`
  when `cookie !== session.user.locale`, so the cookie stays the single request-time
  source for `getRequestConfig`.

### 8. Dates / numbers

- Route locale-aware formatting through next-intl's formatter; update
  `src/lib/format-date.ts` consumers. Keep it deterministic (no bare `toLocale*`).

### 9. Tests (per policy: `src/lib` + `src/actions` only)

- `src/lib/i18n.test.ts`: `resolveLocale` — cookie wins, header fallback, unsupported →
  default, malformed input.
- `src/lib/messages-parity.test.ts`: `fr.json` / `pl.json` key sets match `en.json`.
- `src/actions/i18n.test.ts`: `setLocale` — valid, invalid rejected, DB write when
  authed, cookie-only when anonymous (DB mocked at the boundary).

### 10. Verify

- `npm run build && npm run lint && npm run test`.
- Manual: switch each language and reload (persists); sign out / sign in (follows the
  account); fresh browser with `Accept-Language: fr` → loads in French with no cookie.

### Deferred / out of scope

- URL-based locale routing (`/fr/...`) and its SEO/`hreflang` benefits.
- Translating `ItemType.name` and other seeded/system strings.
- RTL languages (none of en/fr/pl need it).
- Localizing emails (`src/lib/email.ts`) — flag as a follow-up if wanted.

## History
[//]: # (keep this updated. earliest to latest)
[//]: # (Entries were compressed on 2026-08-26 to cut verification narrative, routine build/lint/test-pass notes, and routine "merged into master, branch deleted" notes — decisions, deferrals, bugs found/fixed, and key files were preserved. See git history for full detail on any entry. This file was previously split into context/current-feature-history.md when it exceeded 150k chars, then merged back here the same day once compression brought the combined size well under that threshold.)
- 2026-05-20: Initial Next.js setup via Create Next App
- 2026-07-06: Dashboard UI Phase 1 — shadcn/ui init, `/dashboard` route, dark-mode-default layout, TopBar with search + new item/collection buttons, sidebar/main placeholders.
- 2026-07-06: Dashboard UI Phase 2 — collapsible sidebar (desktop) with item types, favorite/recent collections, user footer; mobile drawer sidebar via Sheet; brand moved to TopBar with responsive mobile layout.
- 2026-07-06: Dashboard UI Phase 3 — stats cards, recent collections grid, pinned/recent items sections with item cards; independently-scrolling sidebar/main; dashboard title + centered content area.
- 2026-07-06: Prisma + Neon PostgreSQL setup — Prisma 7 schema (app + NextAuth models) with driver adapter, initial migration applied to the Neon dev branch, `prisma/seed.ts` translating mock data into seeded rows. UI still read from mock-data.ts, not yet wired to Prisma.
- 2026-07-07: Seed Data — added `password` field to `User` (migration), `bcryptjs`, rewrote `prisma/seed.ts` with demo user/item types/collections/items, idempotent item-collection linking; updated `scripts/test-db.ts`.
- 2026-07-07: Dashboard Collections — added `src/lib/db/collections.ts` fetching collections with items/itemTypes via Prisma; `CollectionsSection` now an async server component using real Neon data; `CollectionCard` border colored by dominant item type.
- 2026-07-07: Dashboard Items — added `getPinnedItems`/`getRecentItems` (`src/lib/db/items.ts`) and `getDashboardStats` (`src/lib/db/stats.ts`); sections now async server components on real data; removed unused `src/lib/dashboard.ts`.
- 2026-07-07: Stats & Sidebar — added `getItemTypes()` (per-type counts) and `getFavoriteCollections()`; `SidebarNav` now prop-driven from real data (item type links, favorite/recent collections, "View all collections" link); fixed the seed's collection upsert (was a no-op on existing rows).
- 2026-07-10: Add Pro Badge to Sidebar — `isPro` flag on `ItemTypeSummary` (derived from type name being file/image); `SidebarNav` shows a "PRO" badge/dot for those types; `/` now redirects to `/dashboard`.
- 2026-07-11: Dashboard Quick Fixes (code-scanner follow-up) — fixed a duplicate `getRecentCollections(6)` query (fetch once, pass as prop), added a `take` limit to `getPinnedItems`, removed a `truncate` class overriding `line-clamp-2` on `ItemCard`.
- 2026-07-28: Auth Setup Phase 1 — installed `next-auth@beta` + `@auth/prisma-adapter`, split config pattern (`auth.config.ts` edge-safe, `auth.ts` full config with JWT strategy), `proxy.ts` protecting `/dashboard/*`. Also added `.mcp.json` to `.gitignore` (had contained a live API key).
- 2026-07-28: Auth Credentials Phase 2 — Credentials provider with real bcrypt validation against Prisma; `POST /api/auth/register` (Zod-validated, hashes password, duplicate-email check). Verified via curl (register, duplicate, mismatch, sign-in success/failure).
- 2026-07-28: Auth UI Phase 3 — custom `/sign-in`/`/register` pages, `SignInForm`/`RegisterForm`/`UserAvatar`; `UserFooter` rewritten as client component with sign-out dropdown and a minimal `/profile` page; `proxy.ts` protects `/profile`; `getCurrentUser()` now reads the real session user.
- 2026-07-28: Email Verification on Register — Resend-based verification email (`src/lib/email.ts`), 24h token via `VerificationToken` model, `GET /api/auth/verify-email`/`POST /api/auth/resend-verification` routes; Credentials `authorize` blocks unverified sign-in via a custom `EmailNotVerifiedError`; new `/check-email` page; `useResendCooldown` (30s) hook shared by both resend buttons.
- 2026-07-29: Toggle Email Verification via Env Flag — `isEmailVerificationEnabled()` (`EMAIL_VERIFICATION_ENABLED`, default on); register/verify/resend routes and Credentials `authorize` all skip verification when disabled. Set to `"false"` locally since Resend has no verified domain yet.
- 2026-07-29: Forgot Password — `src/lib/password-reset-token.ts` reuses `VerificationToken` with a `reset:` prefix (1h expiry, isolated from email-verification tokens); `POST /api/auth/forgot-password`/`reset-password` routes (always return success regardless of email existence, no account-existence leak); new `/forgot-password`/`/reset-password` pages.
- 2026-07-30: Profile Page — expanded the minimal `/profile` into the full spec: `getCurrentUser()` gained `id`/`createdAt`/`hasPassword` (hides password-change UI for GitHub OAuth users); `getProfileStats()`; `change-password`/`delete-account` API routes; `ChangePasswordForm`/`DeleteAccountDialog` (type "delete" to confirm). `/profile` now renders inside the TopBar/Sidebar shell. Also updated `ai-interaction.md` so Playwright verification only happens when explicitly requested.
- 2026-07-31: Rate Limiting for Auth — `@upstash/ratelimit`/`@upstash/redis`, `src/lib/rate-limit.ts` (sliding-window, fails open if Upstash unconfigured); protected register/forgot-password/reset-password/resend-verification by IP, login (5/15min) inside Credentials `authorize()` via a `RateLimitedError`. Needs `UPSTASH_REDIS_REST_URL`/`TOKEN` set in the deploy env to actually enforce.
- 2026-08-01: Items List View — `getItemTypeBySlug`/`getItemsByType`; new `/items/[type]/page.tsx` with a responsive `ItemCard` grid; unknown slugs 404; `proxy.ts` now protects `/items/:path*`.
- 2026-08-01: Vitest Unit Testing Setup — `vitest.config.ts` (node env, scoped to `src/actions/**`/`src/lib/**` only, no DOM/component testing by design); `npm run test`/`test:watch`; example tests for existing `src/lib` utilities. Updated `ai-interaction.md`/`coding-standards.md` workflow/testing docs.
- 2026-08-01: Three-Column Items List Grid — items grid gained `lg:grid-cols-3` for large screens.
- 2026-08-01: Item Drawer — `getItemById`/`GET /api/items/[id]`; `ItemDrawerProvider` (client context) + `ItemDrawer` (Sheet) showing header/action bar (Favorite/Pin/Copy wired, Edit/Delete rendered but not yet wired); `ItemCard` now client component, opens drawer on click, keyboard-accessible. `getItemById` intentionally not scoped by `userId`, matching the rest of the app's then-unscoped queries. Fixed a `tailwind-merge` modifier-stack conflict and an infinite-skeleton bug on failed fetch.
- 2026-08-03: Item Drawer — Edit Mode — first server action, `updateItem` (`src/actions/items.ts`, Zod-validated, ownership-checked via new `getItemOwnerId`); `ItemDrawer` gained inline edit mode (Save/Cancel, type-specific fields). Follow-up: `GET /api/items/[id]` returns a `canEdit` flag disabling Edit for non-owned items; icon tweaks. Added `items.test.ts` (6 tests).
- 2026-08-03: Item Create & Delete — `createItem`/`deleteItem` actions; previously non-functional "New Item" TopBar button now opens `NewItemDialog` (File/Image excluded, out of scope); Trash2 button wired to an `AlertDialog` confirmation, disabled for non-owned items. Added 9 tests.
- 2026-08-03: Verified the shipped Item Create feature against its spec — found link items didn't actually require a URL; fixed via `getItemTypeById` and a client-side Create-button disable. Then polished `NewItemDialog`: fixed the type `Select` showing a raw value instead of the type name, added icons/singular labels, and placeholders across fields.
- 2026-08-04: Code Editor — `@monaco-editor/react`-based `CodeEditor` component (`vs-dark` theme, macOS-style window controls, copy button, auto-grow 120–400px, themed scrollbar); wired into `ItemDrawer`/`NewItemDialog` for snippet/command content only.
- 2026-08-04: Type-Specific Add Button + Preselected Type — each `/items/[type]` page shows an "Add {Type}" button (hidden for files/images); `NewItemDialog` gained a `defaultItemTypeId` prop preselecting/disabling the type `Select`.
- 2026-08-04: Markdown Editor — `MarkdownEditor` (Write/Preview tabs via `react-markdown`+`remark-gfm`, matching `CodeEditor`'s dark theme); wired into prompt/note content. Diagnosed a false "not rendering" report as a stale dev-server CSS bundle, not a real bug.
- 2026-08-06: File Upload with Cloudflare R2 — `src/lib/r2.ts` (S3-compatible client), `src/lib/file-constraints.ts`; `POST /api/upload`/`GET /api/download/[id]`; new `FileUpload` component (drag-and-drop, progress bar); `NewItemDialog`/`ItemDrawer` gained file/image support; `createItem`/`deleteItem` persist file fields and clean up R2 on delete. Found and fixed an unrelated pre-existing dialog "grid blowout" bug (long filenames overflowing `DialogContent`) via `[&>*]:min-w-0`. Added tests for `file-constraints.ts`/`r2.ts` (pure logic only).
- 2026-08-10: Image Gallery View — `ImageThumbnailCard` replaces `ItemCard` on `/items/images` (3-column grid). Iterated on styling per feedback: solid-background footer bar (always-legible title) instead of gradient overlay, type-color dot moved into the footer, hover dim + Expand icon; fixed card height (`h-44`) to match other item-type cards — a deliberate deviation from the spec's `aspect-video` requirement, requested explicitly for grid-rhythm consistency.
- 2026-08-10: File List View — `src/lib/file-icon.ts` (extension→icon mapping, unit-tested); new `FileListRow` component (icon, title/filename, size, date, pin/favorite, Download) replacing the `ItemCard` grid on `/items/files` only.
- 2026-08-10: Quick Copy Icon on Item Cards — `ItemCard` gained a ghost Copy icon button (copies content/url/description to clipboard, toasts). `ImageThumbnailCard`/`FileListRow` left unchanged (already have Download).
- 2026-08-10: Ran a code-scanner audit. Findings: Critical — most item/collection read queries not scoped by `userId` (cross-tenant leak); High — `/api/items/[id]`/`/api/download/[id]` don't check ownership; Medium — unmemoized drawer context, duplicated clickable-card logic, dead `/collections` sidebar link; Low — duplicate `ItemType` scans, rate-limit IP fallback groups headerless clients together.
- 2026-08-11: Fixed Critical/High Security Findings — Per-User Data Isolation — scoped `getPinnedItems`/`getRecentItems`/`getItemsByType`/`getItemById`/`getItemTypes`/`getRecentCollections`/`getFavoriteCollections`/`getDashboardStats` by `userId`; threaded `userId` through the dashboard/items/profile pages; `GET /api/items/[id]`/`GET /api/download/[id]` now pass `session.user.id` in. Verified via Playwright that a second registered user sees an empty dashboard and gets 404s on demo-owned resources.
- 2026-08-11: Ran a second code-scanner audit — confirmed the isolation fix held. New Medium: SVG uploads enable stored XSS, unmemoized drawer context, `ItemDrawer.tsx` grown to 484 lines, duplicated clickable-card logic; New Low: rate-limiter "unknown" IP bucket, no rate limiting on change-password/delete-account.
- 2026-08-11: Fixed Code Scanner Findings (Medium 1/4/5/6 + all Low) — dropped SVG from allowed upload types; memoized `ItemDrawerProvider`'s context; split `ItemDrawer.tsx` (484→~230 lines) into `ItemDrawerView`/`ItemDrawerEditForm`/`DeleteItemDialog`; added shared `useClickableCard` hook; `getRequestIp` falls back to `x-real-ip`; removed a duplicate `getItemTypeBySlug` query; added rate limiting to change-password/delete-account. Deliberately left MIME/extension sniffing and the dead `/collections` links out of scope.
- 2026-08-15: Collection Create — `createCollection` (thin passthrough) + first collections server action (mirrors `createItem`); new `NewCollectionDialog` wired into the previously-dead "New Collection" TopBar buttons. Used a Server Action (not an API route) to match the established mutation pattern, confirmed with the user. Added 3 tests.
- 2026-08-15: Add Item to Collection(s) — `getCollectionOptions(userId)`; new `CollectionMultiSelect` combobox; `createItemSchema`/`updateItemSchema` gained `collectionIds`, re-filtered server-side through a new `getOwnedCollectionIds` (silently drops IDs for collections the user doesn't own, rather than trusting the client). Added 4 tests.
- 2026-08-15: Collections Pages — `/collections` list and `/collections/[id]` detail pages, splitting a collection's items into Items/Images/Files sections since a collection can mix types. `proxy.ts` protects `/collections/:path*`. Found and fixed a case-sensitivity bug in type-name comparisons (`ItemType.name` stored capitalized, not lowercase) via a new `groupItemsByType` pure function, tested for the regression.
- 2026-08-17: Global Search / Command Palette — `getAllItemsForSearch`/`getCollectionsForSearch` feed a new `GlobalSearch` component (shadcn `Command`/`cmdk`, opens via Cmd/Ctrl+K); selecting an item opens its drawer, a collection navigates to it. Fixed two bugs: a missing `<Command>` provider wrapper (this project's `CommandDialog` doesn't supply one, unlike upstream shadcn) and a UI freeze from `cmdk` re-scoring full raw content on every keystroke (fixed via a new `toSearchPreview` helper that truncates/collapses content server-side, tested). Also fixed a right-edge alignment issue via `data-slot="command-shortcut"`.
- 2026-08-17: Pagination — `src/lib/constants.ts`/`src/lib/pagination.ts` (windowed page-range with ellipsis collapsing, 15 tests); `getItemsByType`/`getItemsByCollection`/`getAllCollections` now take `skip`/`take`; wired `PaginationControls` into `/items/[type]`, `/collections`, `/collections/[id]`. Found and fixed a hydration mismatch in `PaginationLink` (a base-ui `data-slot` conflict between `Button` and the render-prop `Link`).
- 2026-08-17: Settings Page — new protected `/settings` page in the shared shell; `UserFooter` dropdown gained a "Settings" item; moved the Security/Danger-Zone cards off `/profile` onto `/settings` (components relocated to `src/components/settings/`, no logic changes).
- 2026-08-17: Editor Preferences Settings — `editorPreferences Json?` on `User` (migration); `src/lib/editor-preferences.ts` (defaults, validating parser); new server action + `GET /api/editor-preferences`; globally-mounted `EditorPreferencesProvider` optimistically applies changes; `CodeEditor` reads font size/tab size/theme/wrap/minimap from it, registers `monokai`/`github-dark` as custom Monaco themes. New "Editor Preferences" card on `/settings`, auto-saves on change.
- 2026-08-17: Favorites Page — `getFavoriteItems`/`getFavoriteCollectionsList`; new `/favorites` page with a compact monospace list (`FavoritesList`, separate Items/Collections sections); star icon added to `TopBar` linking to it.
- 2026-08-17: Favorite Toggle (Drawer, Collection Page, Cards) — `setItemFavorite`/`setCollectionFavorite` + `toggleItemFavorite`/`toggleCollectionFavorite` actions; wired the previously display-only Star buttons across `ItemDrawerView`/`CollectionDetailHeader`/`CollectionCard`, and added new toggles to `ItemCard`/`ImageThumbnailCard`/`FileListRow` — all six use optimistic local state that reverts on failure. Follow-up: swapped `Heart` for `Star` everywhere for visual consistency. Added 8 tests.
- 2026-08-17: Favorites Page Sorting — "Sort by" `Select` (Newest/Oldest/Name A-Z/Z-A/Item Type) on `/favorites`; extracted comparators into `src/lib/favorites-sort.ts` (11 tests) — caught and fixed a real bug in a shared `byDate` helper (an unused parameter had silently broken collections' newest/oldest sort). Fixed a base-ui `SelectValue` label-rendering quirk and a dropdown-alignment issue along the way.
- 2026-08-17: Pinned Items — `toggleItemPin`/`setItemPin` actions, mirroring the favorite-toggle pair; wired the previously static Pin button in the drawer with optimistic state; pinned items now sort to the top via `orderBy` in the relevant queries. Items only, no collection pinning. Added 4 tests.
- 2026-08-17: Homepage Mockup — standalone static marketing prototype at `prototypes/homepage/` (no framework/build step): animated "chaos to order" hero, features grid, AI Pro section, pricing toggle, final CTA, footer — fully responsive, using the spec's placeholder palette rather than the live app's actual `ItemType` colors. Outside `src/`, no app code touched.
- 2026-08-17: Homepage (real) — `src/app/page.tsx` now checks `auth()`, redirecting signed-in users to `/dashboard` and rendering the real marketing homepage for signed-out visitors; ported the prototype into `src/components/homepage/` using real `ItemType` colors/lucide icons instead of the prototype's placeholders. `PricingToggle` folded into one client component after discovering the spec's server/client render-prop split violated the RSC boundary. Follow-up: sign-out now redirects to `/` instead of `/sign-in`.
- 2026-08-18: Page Transition Loading + Reduce Navigation Latency — investigated a "feels slow" report via Playwright: confirmed zero `loading.tsx`/Suspense boundaries anywhere, and every authenticated page awaited `getCurrentUser()` alone before its data batch (2-3 sequential DB round-trips). Added `getSessionUserId()` (DB-free JWT decode) folded into each page's parallel `Promise.all`; added `loading.tsx` to all 7 authenticated routes. First pass regressed the sidebar/topbar (unmounted during the fallback) — fixed by moving those routes into a shared `(app)/layout.tsx` route group so only `<main>` shows the spinner. Verified via throttled-network Playwright testing: warm-pass TTFB dropped from ~400-950ms to ~46-95ms.
- 2026-08-18: Ran the `ui-reviewer` subagent against `/` and `/dashboard`. Along the way fixed a real bug in the agent's own config (a quoted wildcard tool pattern the project's agent-loader doesn't expand, silently running with zero Playwright tools) by spelling out every tool name explicitly. Findings: mobile nav bleed-through at 375px; icon-only card buttons under the accessible touch-target minimum; no `prefers-reduced-motion` fallback on scroll animations; dashboard search wraps at tablet width; homepage footer legal links are placeholders (known/deferred).
- 2026-08-18: Fixed Mobile Nav Overlay + Card Touch Targets (findings 1–2 above) — the mobile nav's dropdown only reached its natural content height while the hero heading sat at a fixed offset, letting a sliver bleed through; fixed via `min-h-[calc(100dvh-4.25rem)]` and forcing a fully opaque header while open, plus a larger hamburger hit area. Bumped icon-only favorite/copy buttons from 24px to the design system's `icon-sm` (28px) variant across `ItemCard`/`FileListRow`/`ImageThumbnailCard`/`CollectionCard`. Reduced-motion, tablet search wrap, and footer links deliberately left out of scope.
- 2026-08-18: Add Homepage Nav to Sign In & Register Pages — `HomeNav` now renders atop `/sign-in`/`/register`; reused the homepage's `Logo` component as the authenticated app's brand mark too, replacing a separate icon+text combo in `TopBar`. A follow-up ask to unify scrollbar styling was dropped after confirming no custom scrollbar CSS exists anywhere to unify.
- 2026-08-18: Ran `/research` on Stripe integration and documented a phased plan, split into Phase 1 (core infrastructure) and Phase 2 (webhooks/checkout/UI/enforcement).
- 2026-08-18: Stripe Integration — Phase 1: Core Infrastructure — migration adding `stripeSubscriptionStatus`/`stripeCurrentPeriodEnd`; `src/lib/stripe.ts` (lazy client) and `src/lib/plan-limits.ts` (pure gating logic, ships but unread until Phase 2); `session.user.isPro` re-synced from the DB on every token validation (accepted per-request DB cost, confirmed with the user) rather than trusting update triggers. Found and worked around a real TypeScript module-augmentation gotcha (`declare module "next-auth/jwt"` silently failed to merge; fixed by targeting `@auth/core/jwt"` directly). Added `.env.example`. Added `plan-limits.test.ts` (12 tests). No enforcement/webhook/checkout/UI wiring yet — deferred to Phase 2.
- 2026-08-18: Stripe Integration — Phase 2: Integration & UI — `POST /api/webhooks/stripe` (signature-verified, syncs `isPro`/status from subscription events); per user decision, a canceled subscription stays Pro until the period actually ends (`.deleted`, not `.updated`, revokes it). Added checkout/portal session routes and a `BillingSection` on `/settings`. Enforcement wired behind `PLAN_GATING_ENABLED` (still off by default): `createItem`/`createCollection` reject over-limit/Pro-only-type creates for non-Pro users. Known inherited limitation: if `subscription.created` arrives before `checkout.session.completed`, `isPro` won't sync until the next `subscription.updated` event. `STRIPE_WEBHOOK_SECRET`/end-to-end checkout testing deliberately deferred by the user.
- 2026-08-18: Reduced demo user seed data to fit the free-tier limit (5→3 collections, 24 items) — reassigned rather than deleted items from the two dropped collections; manually cleaned up stale `Collection` rows the seed script's upsert-only behavior left behind. Investigated a report that limits weren't enforced — confirmed expected: `PLAN_GATING_ENABLED` defaults off per the project's "full access until launch" policy.
- 2026-08-18: Upgrade Page + Subtle Upgrade CTA — fixed the sidebar's "PRO" badge so it only shows for gated types when the signed-in user isn't already Pro; added an "Upgrade" TopBar button (shown only for non-Pro users) linking to a new `/upgrade` page (`UpgradePlans`, Free vs Pro cards, monthly/yearly toggle) that redirects already-Pro users to `/settings`. Extracted Free/Pro feature copy into `src/lib/pricing-features.ts` (derived from the actual limit constants) shared with the homepage. Ran `/cleanup`: fixed a stale routing table in `project-overview.md` and a missing `NEXT_PUBLIC_APP_URL` in local `.env`.
- 2026-08-19: Language Dropdown + Type Select Position Fix — replaced the free-text Language field with a `Select` of 25 common Monaco language ids, moved above Content in both `NewItemDialog`/`ItemDrawerEditForm`; fixed the item-type `Select`'s dropdown alignment (`alignItemWithTrigger={false}`), matching an existing fix elsewhere for the same base-ui quirk.
- 2026-08-19: Ran `/research` on the AI auto-tag spec's "critical gotchas" via Context7 — confirmed `gpt-5-nano` is Chat-Completions-supported but can silently return empty content as a reasoning model when its token budget is exhausted (almost certainly the exact symptom the spec's stricter claim was describing); OpenAI still recommends the Responses API for reasoning models, so the spec's guidance holds. Found and fixed a real contradiction this surfaced in an earlier research doc (`docs/ai-integration-plan.md`) and updated it to match.
- 2026-08-19: AI Auto-Tagging (first AI feature) — `src/lib/openai.ts` (lazy client, `gpt-5-nano`); `src/lib/auto-tag.ts` (input truncation, response parsing for both array shapes); `generateAutoTags` server action (auth → Pro-gate → rate limit 20/hr → Responses API), taking raw title/content rather than an item id since the button is needed pre-creation too. New `SuggestTagsButton` in both dialogs, hidden outright for free users. Found and fixed a real OpenAI API bug via manual testing: the Responses API rejects JSON-object output mode unless the literal word "json" appears in the input itself, not just the instructions — fixed by appending an explicit line to the prompt. Added 19 tests.
- 2026-08-19: AI Description Generator — `src/lib/description.ts` + `generateDescription` action, mirroring the auto-tag action's shape but requesting plain text (sidestepping the "json"-keyword gotcha entirely). New `SuggestDescriptionButton` in both dialogs. Follow-up styling: switched both AI buttons to `variant="outline"`, fixed a vertical icon/text misalignment. Added 18 tests total.
- 2026-08-19: AI Explain Code — `src/lib/explain.ts` + `explainCode` action, same auth/Pro-gate/rate-limit shape, returning markdown (not persisted, regenerated per click). `CodeEditor` gained an Explain action and a Code/Explanation tab switcher, reusing the drawer's markdown preview styling; the editor stays mounted (hidden, not unmounted) to avoid a Monaco remount. Drawer-only (read view), snippet/command types. Added 15 tests total.
- 2026-08-19: AI Prompt Optimizer — `src/lib/optimize-prompt.ts` + `optimizePrompt` action, same shape. `MarkdownEditor` gained an Optimize action for prompt items only (drawer read view), with an Original/Optimized toggle and "Use this version"/"Keep original" footer actions; accepting persists via the existing `updateItem` action. Follow-up fix: guarded against a double-click double-submit with an `isAccepting` state. Added 14 tests total.
- 2026-08-24: Added `.claude/agents/refactor-scanner.md` (a report-only duplication-scanning subagent) and ran it against `src/actions`. Findings: `ActionResult<T>` duplicated 4x, auth-check/Zod boilerplate repeated 14x, ownership-check pattern duplicated 6x, `ai.ts`'s Pro-gate/rate-limit shape repeated 4x.
- 2026-08-24: Refactor: extracted `src/lib/ownership.ts`'s `checkOwnership()`, replacing 6 duplicated ownership-check blocks in `items.ts`/`collections.ts` (`deleteItem` left as-is, needs the full item for R2 cleanup). No behavior change.
- 2026-08-24: Refactor: extracted `src/types/action-result.ts`'s `ActionResult<T>`, replacing 4 identical local declarations. No behavior change.
- 2026-08-24: Refactor: added `isAiProGated()` (`plan-limits.ts`) and shared rate-limit constants, collapsing `ai.ts`'s 4 duplicated Pro-gate blocks (the try/catch/parse wrapper deliberately left as-is per the scan's own over-abstraction caveat). Each AI action keeps its own rate-limit bucket name. No behavior change.
- 2026-08-24: Refactor: added `src/lib/auth-guard.ts`'s `requireSessionUser()`, replacing all 13 duplicated `auth()` session-check blocks across the four actions files — closes out every finding from the `src/actions` scan.
- 2026-08-24: Ran the `refactor-scanner` against `src/components`. Findings: optimistic favorite/pin toggle logic duplicated 6x; comma-separated tags parsing duplicated 6x (including a verbatim-duplicate function); pricing toggle UI duplicated 3x plus a duplicated Stripe-checkout fetch; "Back to sign in" link duplicated 3x; dashboard grid-section wrapper shared by 3 sections.
- 2026-08-24: Refactor: extracted `useOptimisticToggle` hook, replacing the duplicated toggle logic in 5 of the 6 flagged components — `ItemDrawer.tsx`'s handlers deliberately left out of scope on closer inspection (they mutate a nested field of a larger object, not an owned boolean).
- 2026-08-24: Refactor: extracted `FeatureList`/`PricingIntervalToggle` components and `src/lib/stripe-client.ts`'s `startProCheckout()`, consolidating the pricing toggle and checkout-redirect fetch across the homepage, `/upgrade`, and `/settings` billing — no visual change at any of the 3 sites.
- 2026-08-24: Refactor: extracted `src/lib/tags.ts`'s `parseTagsInput`/`appendTagToInput`, replacing all 6 inline occurrences including a previously byte-for-byte-duplicate `handleAcceptTag` function. Added 7 tests.
- 2026-08-24: Refactor: extracted `BackToSignInLink` and `DashboardGridSection` components, addressing the two remaining low-impact findings from the `src/components` scan and closing it out entirely.
- 2026-08-24: Ran the `refactor-scanner` against `src/lib/db`. Findings: `ItemDetail` mapping duplicated 3x in `items.ts`; identical pagination boilerplate between `getItemsByType`/`getItemsByCollection`; a lower-priority shared query shape between `getItemTypes`/`getProfileStats`.
- 2026-08-24: Refactor: extracted `toItemDetail`/`itemDetailInclude` in `items.ts`, replacing the 3 duplicated mapping blocks.
- 2026-08-24: Refactor: extracted `getPaginatedItemsByWhere`, closing out the two recommended findings from the `src/lib/db` scan (the third was flagged opportunistic-only).
- 2026-08-24: Ran the `refactor-scanner` against `src/hooks` — all 3 existing hooks already well-adopted; one finding: `ItemDrawer.tsx`'s favorite/pin handlers hand-roll the pattern `useOptimisticToggle` already centralizes (a deliberate earlier exclusion). Asked the user whether to revisit it; confirmed yes.
- 2026-08-24: Refactor: generalized `useOptimisticToggle` with an exported `toggleOptimisticField()` helper (for callers whose boolean lives inside larger state, no internal `useState`) and adopted it in `ItemDrawer.tsx` — closes out the `src/hooks` scan.
- 2026-08-24: Ran the `refactor-scanner` against `src/app/api`. Findings: identical `auth()`+401 block repeated 7x; identical Zod-error-response shaping repeated 6x; rate-limit-check-and-429 block repeated 6x (lower priority); duplicated `VerificationToken` delete pattern 4x; a duplicated Stripe `APP_URL` fallback constant.
- 2026-08-24: Refactor: extracted `requireApiSessionUser()` (`auth-guard.ts`), replacing the 7 duplicated auth+401 blocks across the API routes.
- 2026-08-24: Refactor: extracted `src/lib/api-response.ts`'s `zodErrorResponse()`, replacing 6 duplicated blocks.
- 2026-08-24: Refactor: extracted `src/lib/verification-token-store.ts`'s `deleteVerificationToken()`, replacing 4 duplicated Prisma delete calls across the verify-email/reset-password routes.
- 2026-08-24: Refactor: extracted `enforceRateLimit()` (`rate-limit.ts`), replacing 6 duplicated check-and-429 blocks.
- 2026-08-24: Refactor: folded the duplicated Stripe `APP_URL` fallback into `stripe.ts` — closes out every finding from the `src/app/api` scan.
- 2026-08-24: Ran the `refactor-scanner` against `src/app` (pages/layouts + a re-check of `src/app/api`, which came back clean). Findings: item-grid renderers duplicated between `items/[type]`/`collections/[id]`; repeated page-wrapper class strings (low priority); duplicated `singularize` helper; a duplicated "centered auth card" skeleton across all 5 auth pages.
- 2026-08-24: Refactor: extracted `ItemCardGrid`/`ImageThumbnailGrid`/`FileListGroup` grid-renderer components, replacing duplicated grid markup in `items/[type]`/`collections/[id]`.
- 2026-08-24: Refactor: extracted `AuthCard` component, replacing the identical Card skeleton across all 5 auth pages (each keeps its own outer wrapper/content class as before).
- 2026-08-24: Refactor: extracted `singularize` into `src/lib/text.ts` — closes out the `src/app` scan (the page-wrapper-classes finding was flagged optional and left unaddressed).
- 2026-08-24: Ran the `refactor-scanner` against `src/components/dashboard` (a narrower re-pass). Findings: favorite-toggle logic + Star button JSX duplicated 3x; collection name/description fields identical between two dialogs; collection edit/delete dialog state duplicated between `CollectionCard`/`CollectionDetailHeader`; item create/edit form fields (~125 lines each) duplicated between `NewItemDialog`/`ItemDrawerEditForm` (largest finding).
- 2026-08-24: Refactor: extracted `ItemFormFields` (top-priority finding) — the shared Title→Description→Language→Content→URL→Tags sequence, using per-field callbacks since the two forms' state shapes differ; the Type field and `CollectionMultiSelect` deliberately stay outside it (rendered differently in each form). `NewItemDialog` (324→239 lines), `ItemDrawerEditForm` (191→88 lines).
- 2026-08-24: Refactor: extracted `useItemFavoriteToggle` hook and `FavoriteToggleButton` component, replacing the duplicated favorite logic in `ItemCard`/`ImageThumbnailCard`/`FileListRow`.
- 2026-08-24: Refactor: extracted `CollectionFormFields`, replacing identical Name/Description fields in `NewCollectionDialog`/`EditCollectionDialog`.
- 2026-08-24: Refactor: extracted `useCollectionDialogs` hook and `CollectionDialogs` component, replacing duplicated dialog-pair state/JSX in `CollectionCard`/`CollectionDetailHeader` (each keeps its own trigger UI and `onDeleted` callback) — closes out the entire `src/components/dashboard` scan.
- 2026-08-24: Sidebar Active Item Highlighting — `SidebarNav.tsx` is now a client component (`usePathname()`) highlighting the active item-type/favorite-collection/recent-collection/"View all collections" link. Decision: a collection that's both favorited and recent highlights in both sections (kept, not deduped — both are legitimate independent links), confirmed with the user. UI-only, no new tests.
- 2026-08-24: Fixed a spurious outer scrollbar on the item drawer for large-markdown Notes — `MarkdownEditor.tsx`'s `TabsContent` panels were contributing their full unclipped height to the enclosing Sheet's `scrollHeight` despite being visually clipped (a nested-flex/overflow browser quirk). Fixed via `[contain:layout]` on the Write/Preview `TabsContent` panels — the only `TabsContent` usage in the codebase; `CodeEditor.tsx` (Monaco) unaffected. UI-only CSS fix, no new tests.
- 2026-08-25: Dashboard Redesign — "Ledger" Design System (Desktop) — adopted design tokens from `context/redesign/kept-dashboard.html` app-wide in `globals.css` (dark-mode hex palette mapped onto existing shadcn vars, new `--ink-body`/`--rule-strong` tokens, `--radius: 0`); light `:root` values stubbed for a future toggle, unused. Swapped Geist/Geist Mono for JetBrains Mono (`layout.tsx`). Rebuilt the "collapsed hairline border" grid pattern (`StatsCards`, `DashboardGridSection`, `ItemCardGrid`). Restyled `CollectionCard`/`ItemCard` (shared with `/items/[type]`, `/favorites`, `/collections`) to plain grid-cell divs with `border-l-2` accent stripes (kept vivid per-type/collection colors), soft-tint chips via new `withAlpha()` (`src/lib/color.ts`), relative-time badges via new `formatRelativeTime()` (`src/lib/relative-time.ts`). Restyled `ItemDrawer`/`ItemDrawerView` to match; `ItemDrawerEditForm.tsx` intentionally left unstyled (not in the mockup). Kept lucide icons (not the mockup's hand-drawn SVGs); omitted the mockup's "Last opened/ID" drawer footer (needs new schema tracking). Flagged, not fixed: pre-existing hydration mismatch on `/favorites` (`FavoritesList.tsx`'s `toLocaleDateString()`). Mobile dashboard redesign deferred. Added `color.test.ts`/`relative-time.test.ts`. Pushed to `feature/dashboard-redesign`, left unmerged per request.
- 2026-08-25: Auth Redesign — "Ledger" Design System (`/sign-in`, `/register`), branched off dashboard-redesign. Source: `kept-auth.html`. Kept `HomeNav` (already token-compatible) instead of the mockup's minimal bar. `AuthCard.tsx` (shared by all 5 auth pages) dropped shadcn `Card` for a plain accent-striped div, gained optional `crumb`/`stepLabel` breadcrumb props. Restyled `SignInForm`/`RegisterForm` labels/inputs/buttons — no field/validation/logic changes. `forgot-password`/`reset-password`/`check-email` inherit `AuthCard`'s restyle automatically (accepted side effect, not separately redesigned since no mockup covers them). Pushed to `feature/auth-redesign`, left unmerged per request.
- 2026-08-25: New Item/New Collection Dialog Redesign — no mockup exists; extrapolated already-established patterns. Restyled at call sites (`NewCollectionDialog`/`NewItemDialog`) rather than editing shared `ui/dialog.tsx`. Label/Input/Textarea/Select restyled in shared `CollectionFormFields.tsx`/`ItemFormFields.tsx` (also used by `EditCollectionDialog`/`ItemDrawerEditForm` — accepted ripple) via `LABEL_CLASS`/`FIELD_CLASS` constants. `SelectContent`/`Command`/`Popover`/`FileUpload`/`CodeEditor`/`MarkdownEditor` internals left untouched (shared more broadly). Pushed to `feature/create-dialogs-redesign`, left unmerged per request.
- 2026-08-25: Documented `context/design-system.md` — the reference doc replacing mockups going forward (per explicit decision, no more mockups will be produced). Distills the ledger system (tokens, layout patterns, form conventions, `withAlpha`/`formatRelativeTime`, 6 engineering principles) from the three completed redesign passes, plus a status table of done/partial/not-started screens. Wired into `CLAUDE.md`'s context-file list and `project-overview.md` §10. Docs only.
- 2026-08-25: Reduce Pro Gating for Portfolio — lowered `FREE_ITEM_LIMIT` (50→5) and `FREE_COLLECTION_LIMIT` (3→1) in `plan-limits.ts` so the Pro gate is reachable quickly for portfolio visitors. Added `activateDemoPro`/`deactivateDemoPro` server actions (`src/actions/billing.ts`) — a portfolio-only bypass flipping `User.isPro` directly, alongside (not replacing) the real Stripe flow; refuses to deactivate for users with a real `stripeCustomerId`. Wired into `/upgrade` and `/settings` Billing. `PLAN_GATING_ENABLED` itself unchanged (still needs enabling in the deploy env). Deferred: Stripe test-card UI hint, README/LICENSE rewrite. Added `billing.test.ts` (4 tests).
- 2026-08-25: DevStash → Kept rebrand (name chosen after brainstorming several directions; rejected "My Depot"/"Silo"). Replaced all user-facing "DevStash" strings with "Kept" (`layout.tsx`, auth/settings copy, homepage sections, `email.ts`), reworded two orphaned "stash" puns, `package.json` name. Seed/scripts now use `demo@kept.app`; ran a scoped Neon UPDATE on the `development` branch so the live demo login matches immediately. Deliberately left untouched: history log entries above this line, dated specs under `context/features/`/`context/research/`, and the Neon MCP section's literal `devstash` project id (external identifier, not branding). GitHub repo rename deferred until after merge. Pure text rename + one scoped data UPDATE, no schema/logic changes.
- 2026-08-25: Merged the full ledger-redesign branch chain into `master` — `feature/dashboard-redesign` → `feature/auth-redesign` → `feature/create-dialogs-redesign` → `docs/design-system-reference`, all cleanly (each branched from the tip of the previous). Local and remote branches deleted after merge.
- 2026-08-25: Homepage Redesign — Positioning + Ledger System (`context/features/homepage-redesign-spec.md`), the last pre-redesign screen. Positioning: dropped "developer knowledge hub" framing across hero/features/CTA/footer copy; new tagline "Keep Everything. Find Anything." Visual: moved `/` onto the ledger system — `Logo.tsx` (also fixes `TopBar`'s mark), `HomeNav.tsx` (dropped blur-on-scroll), `HeroSection`, `ChaosToOrder` (left-edge accent stripes on mini cards, kept the "chaos" side's messiness intentionally as the "before" half), `FeaturesSection` (collapsed-hairline grid), `AiSection`, `PricingToggle` (`FeatureList.tsx`/`PricingIntervalToggle.tsx` left untouched, shared with not-yet-redesigned `/upgrade`/`/settings`), `FinalCtaSection`, `Footer`. Flagged, not fixed: every `Button` renders at a 10px computed radius instead of the claimed 0px (pre-existing, likely a `--radius` resolution issue between `globals.css` and shadcn's own CSS). Follow-ups: added smooth-scroll anchor nav; fixed a real bug in `ChaosToOrder`'s mouse-repel animation (unfloored distance divide could spike toward infinity near the cursor) and replaced symmetric-noise ambient movement with a wander-steering model, then decoupled ambient drift speed from repel-push feel (separate constants). Merged `feature/homepage-redesign` into `master`, branch deleted (never pushed).
- 2026-08-25: Moved `context/redesign/` (mockup HTML files) to `prototypes/redesign/`, matching the `prototypes/homepage/` convention; updated the two path references (`design-system.md`, a `globals.css` comment).
- 2026-08-25: Implemented the user-selected direction (1b: bookmark-with-checkmark) from `prototypes/redesign/Kept Logo.dc.html`'s 3-option canvas as the real `Logo.tsx` — hairline SVG colored via `text-primary`. Shared by `HomeNav`/`Footer`/`TopBar`, all updated automatically.
- 2026-08-25: Increased logo size on the authenticated app's `TopBar` — `Logo.tsx` gained a `size?: "default" | "lg"` prop (default unchanged for `HomeNav`/`Footer`); `TopBar` now uses `lg`.
- 2026-08-25: Increased logo size on the landing page too — `HomeNav.tsx` now uses `size="lg"`, rippling into `/sign-in`/`/register`. `Footer.tsx`'s separate `<Logo />` usage stays default size, not covered by this change.
- 2026-08-26: Sidebar Redesign — "Ledger" Design System, closing a scope cut from the dashboard redesign pass (`Sidebar`/`SidebarNav` chrome was left unstyled). Confirmed `prototypes/redesign/support.js` is just the generic Claude Design canvas runtime, not app styling to port. Scoped to sidebar chrome only via `AskUserQuestion` — TopBar buttons/search, `UserFooter`'s avatar, and a Light/Dark toggle deferred (real new functionality). `SidebarNav.tsx`: uppercase/tracked section labels with dotted dividers, `withAlpha()`-tinted icon chips, uppercase item-type names (collections stay Title Case — user content vs. static labels), zero-padded/tabular counts, zero-count types dim instead of hide, favorite/recent-collection markers swapped from star/circle to accent-colored squares. Ripples into `MobileSidebar` (shares `SidebarNav`). Known minor inconsistency, not fixed: `MobileSidebar`'s own "Quick Actions"/"Navigation" headers stayed in the old style. Merged `feature/sidebar-redesign` into `master`, branch deleted (never pushed).
- 2026-08-26: Documented `context/features/topbar-footer-theme-spec.md` — remaining sidebar-chrome follow-up (TopBar buttons/search, `UserFooter` avatar shape, Light/Dark toggle), flagging the toggle as real new functionality with open questions (library vs. hand-rolled, persistence) rather than assuming an approach. Docs only.
- 2026-08-26: User Menu — Account Popup + Light/Dark Toggle. Found `next-themes` already installed but never mounted; added a `ThemeProvider` (`layout.tsx`, `attribute="class"`, `defaultTheme="dark"`, `enableSystem={false}`), replacing the hardcoded `.dark` class. Restyled `UserFooter.tsx`'s dropdown (Account label, Profile/Settings, new Appearance section with a Light/Dark toggle, Sign out). Added `UserAvatar.tsx`'s `shape?: "circle" | "square"` prop (only `UserFooter` uses square) and `useHasMounted.ts` hook to guard the toggle highlight against a hydration mismatch. Ran a live `ui-reviewer` pass in light mode (first time it was reachable) and fixed 3 rounds of findings: (1) `withAlpha()` chips hardcoded the dark alpha suffix — added `useSoftTintAlpha()` across all 4 call sites, and retuned `:root`'s `--border`/`--sidebar-border` for contrast (deliberate departure from the mockup's own light value; dark mode untouched); (2) the collapsed-border-grid's empty trailing cells showed as a visible gray hole in light mode — added `computeFillerClasses()`/`GridFillerCells.tsx` (unit-tested) to `ItemCardGrid`/`DashboardGridSection`; (3) that fix missed `PinnedItemsSection`/`RecentItemsSection` and a related row-height inconsistency — fixed by rendering `GridFillerCells` unconditionally and threading `h-full` through. Added 6 new `grid-filler.ts` tests. Merged `feature/user-menu-theme-toggle` into `master`, branch deleted (never pushed).
- 2026-08-26: Toast Redesign — "Ledger" Design System. Source: `prototypes/redesign/Kept Toasts.dc.html`. Restyled the sonner `Toaster` (`sonner.tsx`) to a flat bordered panel with an accent left-stripe — two variants only (success/error), dedicated tokens in `globals.css`, `unstyled: true` (a few sonner-internal rules still needed `!important` to lose, documented inline). Position/width tuned to the mockup, responsive to bottom-center/full-width on mobile via `useIsMobile()`. Skipped per `AskUserQuestion`: the mockup's meta code badge (no such code system exists) and action-button row (would be new functionality). Accepted deviation: no per-type toast duration (sonner limitation). Follow-up: added `src/lib/toast.ts` wrapper so titles are generic status words ("Success"/"Error") with the message as description — all 23 `sonner` import sites switched to it (mechanical, call signatures unchanged). Known accepted edge case: a toast fired over a full-screen mobile dialog lands near its footer button instead of floating free. Documented in `context/design-system.md`. Merged `feature/toast-redesign` into `master`, branch deleted (never pushed).
- 2026-08-26: Auth Pages Polish (Breadcrumb, Centering, Nav Links) — `AuthCard`'s breadcrumb row simplified to a single label (dropped the "Access /" prefix and "N fields" step count; reworked to render `crumb` alone without requiring `stepLabel`), register's crumb reworded "Register" → "Sign up". `/sign-in`/`/register` now center the form vertically as well as horizontally (`absolute inset-0` centering instead of `pt-24`). `HomeNav`'s `Features`/`Pricing` links pointed at `/#features`/`/#pricing` so they resolve correctly when rendered off the homepage. Also picked up an unrelated small fix already staged: `cursor-pointer` on `PricingIntervalToggle`'s track. Visual/wording-only, no logic changes.
- 2026-08-26: Colored Icons on Homepage Supported-Types Strip — `SupportedTypesStrip.tsx` icons (Snippets/Prompts/Commands/Notes/Links/Files/Images) now render in each type's real accent color inside a `withAlpha()` soft-tint chip (per `AskUserQuestion`), matching `ItemCard`/`SidebarNav`'s chip pattern; became a client component for `useSoftTintAlpha()`. This reverses a prior deliberate choice (the strip was intentionally desaturated as an "understated" list, not a marketing claim) per explicit request.
- 2026-08-26: Sidebar Font-Size Fixes — a live-app check against `prototypes/redesign/kept-dashboard.html` found sidebar text rendering at inherited Tailwind defaults (14px/12px) instead of the mockup's 11px/10px spec. `SidebarNav.tsx`: item-type/collection-name rows `text-sm`→`text-[11px]` (per `AskUserQuestion`, scoped to the whole nav, not just item types). `UserFooter.tsx`: dropdown items, footer name/email resized to match; `UserAvatar.tsx` gained an optional `fallbackClassName` prop so the square-variant initials (used only by `UserFooter`) could be sized independently of the shared primitive's `text-sm` default, leaving other `UserAvatar` call sites (e.g. `/profile`) untouched. Verified via Playwright computed-style checks against the mockup's literal `font-size`/`letter-spacing` values.
- 2026-08-26: Collection Favorite Star — Clickable Toggle — a user question about `CollectionCard`'s static green square (its favorite indicator) led to swapping it for a clickable `Star` (green fill, matching the ledger accent) per `AskUserQuestion`, then making it an actual toggle rather than display-only. `FavoriteToggleButton.tsx` (shared with items) gained an optional `color` prop (`"yellow"` default, `"primary"` new) instead of duplicating the toggle button; `CollectionCard.tsx` now uses it directly and dropped the now-redundant Favorite/Unfavorite item from its `⋮` dropdown (Edit/Delete only). Also added success toasts everywhere favoriting happens app-wide (per `AskUserQuestion`, scoped beyond just this card): `useOptimisticToggle.ts` gained an optional `onSuccess` callback threaded through to the existing `toggleOptimisticField` (item pin already used this pattern, favoriting didn't) — wired into item cards/rows, the item drawer, and both collection favorite entry points (`CollectionCard.tsx`, `CollectionDetailHeader.tsx`). Two separate branches (`fix/sidebar-font-sizes`, `fix/collection-favorite-star`), each merged into `master` and deleted.
- 2026-08-26: Cursor Pointer Audit — All Clickable Elements — a full sweep found the gap was systemic rather than scattered: verified via computed-style checks that every button in the app rendered `cursor: default` (shadcn's `Button` primitive never set `cursor-pointer`), so fixed it at the primitive level instead of per call site. `ui/button.tsx`, `checkbox.tsx`, `switch.tsx`, `tabs.tsx` (TabsTrigger), `select.tsx` (SelectTrigger) gained `cursor-pointer` on their base classes, rippling app-wide automatically. Deliberately left `DropdownMenuItem`/`SelectItem`/`CommandItem` rows at `cursor-default`, matching the native-menu convention already used consistently elsewhere in the app. Also patched 8 raw `<button>` elements that bypass the `Button` component and so missed the primitive fix: `UserFooter.tsx` (user-menu trigger, Light/Dark toggle), `SuggestTagsButton.tsx` (accept/reject tag icons), `MarkdownEditor.tsx` (Original/Optimized toggle), `GlobalSearch.tsx` (search bar trigger), `MobileTabBar.tsx` (Items tab) — confirmed `PricingIntervalToggle.tsx` and the `useClickableCard`-based cards already had it from earlier piecemeal fixes. Verified live via Playwright across `/dashboard` (desktop + mobile viewport), `/settings`, and the New Item dialog's collection checkbox. Merged `feature/cursor-pointer-audit` into `master`, branch deleted.
- 2026-08-26: Item Card Content Preview Fix — a user-supplied screenshot of `/items/links` and `/items/snippets` showed the content preview box crowding multi-line code. Root cause was two-fold: `ItemCard.tsx`'s preview box was fixed at `h-[58px]` (~3 lines) and had no `white-space` override, so newlines/indentation in code content were collapsing into one run-on line under the default `normal` value. Fixed via `AskUserQuestion` (chose the low-risk options over wider-columns/syntax-highlighting alternatives): bumped the box to `h-[92px]` (~5 lines) and added `whitespace-pre-wrap break-words` to preserve line breaks while still wrapping long unbroken tokens (e.g. URLs) inside the fixed-width box. Shared by all five text-based item types via `ItemCardGrid`. No new dependency, no logic change.
- 2026-08-27: UI Review (dark-mode `/dashboard` + create/favorite flows) + fixes. Ran the `ui-reviewer` subagent; it couldn't reach the item drawer / `/collections/[id]` because every dynamic route 404'd in the running dev server (stale `.next`, environmental — not a redesign regression; deferred a re-review to after a clean rebuild). Shipped fixes on `fix/dashboard-ui-review-findings`: New Item/New Collection `DialogContent` gained `max-h-[90vh] overflow-y-auto` (desktop dialog was 910px tall with no scroll, clipping the header/CREATE below ~1366px); `aria-label` + `aria-hidden` on `ItemCard`/`CollectionCard` (the `role="button"` card was dumping ~8KB of content into its accessible name); the desktop shell breakpoint moved `md:`→`lg:` across `TopBar`/`Sidebar`/`MobileTabBar`/`(app)/layout.tsx` so tablet (768–1023px) uses the mobile layout instead of a cramped 3-col topbar (reverses the 2026-08-18 "out of scope" note); favorite toggles now `router.refresh()` (`useItemFavoriteToggle`, `CollectionCard`, `CollectionDetailHeader`) so the stats strip + sidebar favorites stop going stale; desktop toast offset `top: 20`→`76` to clear the topbar search; new styled `src/app/not-found.tsx`. Smaller: `ItemCard` preview box now hidden when content is empty + a bottom fade mask; copy button falls back to title for content-less items; dashboard eyebrow "Register / N entries" → "Ledger / N records"; `Heart`→`Star` in the stats strip; stronger `disabled:opacity-40` on the mobile New Item CTA. Deliberately skipped (agreed): the FAB mid-scroll float (`pb-36` already clears the last card), a "silent drawer failure" toast (`ItemDrawer` already handles it), the dark-mode filler-cell tint, and next-themes persisting a prior light-mode choice. Not done: labelling the stray hidden base-ui `Select` form input — needs a change to shared `ui/select.tsx`, which the design-system rules keep off-limits. Also deleted 21 stray review screenshots the agent wrote to the repo root. UI/wiring/wording only — no new tests.
- 2026-08-27: UI Review follow-up — item drawer + `/collections/[id]` (the surfaces the first pass couldn't reach; the environmental route-404s had cleared by then). Fixes on `fix/drawer-collection-review-followup`: new `src/lib/format-date.ts` (`formatDate` — locale + `timeZone: 'UTC'` pinned) replacing bare `toLocaleDateString()` in `FileListRow`, the item-drawer header, and `FavoritesList` — those were throwing React hydration mismatches (server vs. client locale); added `format-date.test.ts` (4 tests). A11y: `aria-label` on the drawer's Edit and Delete buttons (`ItemDrawerView`/`DeleteItemDialog` — icon-only, previously announced as bare "button"), the Monaco copy-code button (`CodeEditor`), and the `CollectionMultiSelect` search input (placeholder-only before). Consistency: `EditCollectionDialog` chrome brought in line with `NewCollectionDialog` (it was missed by the 2026-08-25 create-dialog redesign — only shared the form fields); the orphaned bold "Type" label in `ItemDrawerEditForm` restyled to match its `LABEL_CLASS` siblings (rest of that form stays known-unstyled). Polish: fixed-height muted box around the drawer image preview so a large image loading in no longer collapses then jumps the layout; strengthened `ItemCard`'s content-preview bottom fade (`mask-b-from-80%` → `55%`) — verified the mask utility actually emits CSS in Tailwind 4.3. Confirmed the base-ui `Select` hidden form input is already `aria-hidden`/`tabIndex=-1` in v1.6.0, so the earlier "L8" finding needed no change. Deferred as separate design decisions: the drawer read-view content preview staying capped at 400px inside a taller drawer, and the weak visual weight of the destructive confirm button (both touch shared components / global tokens). Left as known-unstyled: `/collections/[id]` section headers, the edit-form Save/Cancel bar stickiness, h1→h3→h4 heading skips. UI/a11y only, one new util + tests.
- 2026-08-27: Route-aware mobile FAB — the mobile `MobileTabBar` "+" FAB (rendered once in `(app)/layout.tsx`, always opened New Item) now adapts by route: `/collections` opens New Collection; `/collections/[id]` opens New Item with that collection preselected; `/items/[type]` opens New Item locked to that type (parity with the desktop per-type "Add {Type}" button); everywhere else unchanged. `NewItemDialog` gained a `defaultCollectionIds?: string[]` prop mirroring `defaultItemTypeId` — preselected but not locked, filtered against `collectionOptions` for safety; its form-reset now targets a computed `initialForm` instead of the bare `EMPTY_FORM` constant. The FAB keys `NewItemDialog` by `typeId:collectionId` so it remounts on navigation — the persistent layout mount would otherwise freeze the `useState` form defaults at their first-render value. Non-obvious: `/items/files` and `/items/images` now preselect the (Pro-gated) File/Image type; `createItem` still enforces gating server-side, and this is strictly better than the old default-to-Snippet. Route parsing is done with regexes on `usePathname()` rather than new props from the layout. Components-only, no new tests per the testing policy.
- 2026-08-27: Cleanup pass — styling + deduplication, folder by folder over `src/` (`/cleanup` checks were clean; `src/actions`/`src/app/api` already deduped by the 2026-08-24 passes; `src/components/ui/*` untouched per design-system rules). Two fixes with behavior impact: `useIsMobile` `767px`→`1023px` (app chrome moved to `lg` on 2026-08-27, so 768–1023px toasts + `NewItemDialog` sheet-vs-dialog were mis-classed as desktop), and `BillingSection`'s bare `toLocaleDateString()` in a client component → `formatDate` (same hydration-mismatch class fixed elsewhere the same day; date now renders `1 Sep 2026`). Extractions: `useCollectionFavoriteToggle` hook (inlined identically in `CollectionCard` + `CollectionDetailHeader`), `useProCheckout` hook (`BillingSection` + `UpgradePlans`), `AuthSwitchLink` component, `ItemDrawer`'s `applyItemUpdateResult` helper; plus `isProOnlyType`/`getUserStripeCustomerId`/`withAlpha` reused instead of local copies, verbatim class strings hoisted to consts, `MobileSidebar` headers onto ledger `SECTION_LABEL_CLASS`, `ItemCardGrid` `md:`→`sm:`, `collections/[id]` 3× section block → map. Net −39 lines over 18 files + 3 new; no new tests (components/hooks, per policy).
- 2026-08-27: Mobile shell viewport scroll fix — on `/items/[type]` and `/collections` (any long list), scrolling moved the whole page instead of staying inside `<main>`: the TopBar scrolled away and content slid under the fixed `MobileTabBar`. Cause: `(app)/layout.tsx`'s shell was `h-screen` (`100vh`), which on mobile browsers is the *large* viewport (ignores the collapsing address bar), so the shell exceeded the visible area and the body overflowed. Fixed by `h-screen` → `h-dvh` (dynamic viewport height tracks the actually-visible height); `<main>`'s existing `overflow-y-auto` then contains the scroll. One-line change, global to the `(app)` group. No new tests (layout-only).
- 2026-08-27: Homepage theme toggle — added a Light/Dark control to the marketing nav (`HomeNav`, also rendered on `/sign-in` and `/register`) so signed-out visitors can switch themes; previously the only theme control was in the authenticated app's `UserFooter` dropdown. New `src/components/homepage/ThemeToggle.tsx` — landed first as a Sun/Moon icon button, then (per follow-up request) reworked to a Sun/`Switch`/Moon control (checked = dark; the flanking icon dims to `text-muted-foreground` on the inactive side), placed before the Sign In button. Reuses the existing `next-themes` provider and `useHasMounted` guard (renders as checked/dark until mounted, matching server markup to avoid a hydration mismatch). No provider changes, no new dependency, no new tests (component-only, per policy).
