# Kept

A developer knowledge hub for snippets, commands, prompts, notes, files, images, links and custom types.

## Context files

Read the following to get the full context of the project:

- @context/project-overview.md
- @context/coding-standards.md
- @context/ai-interaction.md
- @context/current-feature.md

## Commands

```bash
npm run dev      # start dev server
npm run build    # production build
npm run start    # start production server
npm run lint     # run ESLint
npm run test     # run unit tests (server actions + utilities)
npm run test:watch # run unit tests in watch mode
```

## Neon MCP

- Project: `devstash` (id: `silent-water-52033547`)
- Default branch for all Neon MCP operations: `development` (id: `br-spring-queen-asd9go7f`)
- ALWAYS target the `development` branch unless I explicitly say "production" or name it directly.
- NEVER run destructive SQL (DROP, DELETE, TRUNCATE, UPDATE without WHERE) or touch the `production` branch (id: `br-gentle-king-asxnqbez`) without explicit confirmation from me first.



